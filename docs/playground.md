---
title: Playground
description: Try csv-pipe live in your browser. Encode objects to CSV and parse CSV into records with the real library, no install.
---

# Playground

Encode objects to CSV and parse CSV into records, live. This runs the real
csv-pipe in your browser, so what you see is exactly what the library produces.

<Playground />

Ready to use it for real? See [Getting started](/guide/getting-started), the
[examples](/guide/examples), or the [API reference](/api/).
